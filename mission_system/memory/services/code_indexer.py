"""MOVED: Code indexer service has been moved to the capability layer.

Import from jeeves_capability_hello_world.database.services.code_indexer instead.

This file is intentionally left as a stub. No backward-compatibility shim is
provided -- callers must update their imports to the new location.
"""

raise ImportError(
    "CodeIndexer has moved to jeeves_capability_hello_world.database.services.code_indexer. "
    "Update your import."
)
